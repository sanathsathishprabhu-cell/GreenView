// Firebase Admin SDK is not configured yet.
// These are no-op stubs so the app builds without credentials.
export const adminDb = {} as never;
export const adminAuth = {} as never;
export const adminStorage = {} as never;
export default {};
